  // const streamCamera = new StreamCamera({
  //   codec: Codec.MJPEG,
  //   flip: Flip.Vertical,
  //   sensorMode: SensorMode.Mode6
  // })

  // streamCamera.on('frame', (data) => {
  //   // socket.binary(true).emit('pi-video-stream', CAMERA_ID, "data:image/jpeg;base64," + data.toString("base64"));
  //   console.log('streamCamera on frame called',)
  // })
  // await streamCamera.startCapture()